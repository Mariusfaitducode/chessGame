#ifndef ENUM_H
#define ENUM_H


enum class Color
{
    blanc, noir
};

enum class TypePiece
{
    pion, cavalier, fou, tour, dame, roi
};

enum class StatuPiece
{
    selected, blocked, dead
};


#endif // ENUM_H
